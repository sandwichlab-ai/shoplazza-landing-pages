<style>
  .add_address {
    margin-top: 40px;
    padding: 15px 20px;
  }
  .address_nav {
    order: -10;
  }
  .address_title {
    order: -20;
  }
  .address_item_container {
    padding: 24px 30px;
    border: 1px solid var(--color-border);
    height: 264px;
  }
  .default_tag {
    font-size: 12px;
    position: absolute;
    right: 0;
    top: 0;
    width: 0;
    height: 0;
    border-top: 40px solid var(--color-primary-btn-bg);
    border-left: 40px solid transparent;
  }
  .default_tag_svg {
    color: var(--color-primary-btn-text);
    right: 5px;
    top: -37px;
    position: absolute;
  }
  .address_item_content {
    color: rgba(159,159,159);
    row-gap: 4px;
  }
  .addresses_list {
    display: grid;
    grid-template-columns: repeat(3,minmax(0, 1fr));
    margin: 50px 0 64px;
    gap: 40px;
  }
  .address_form_header {
    margin: 116px 0 36px;
  }
  .addresses_list[finish][data-empty] ~ .address_empty {
    display: block;
  }
  .addresses_list[finish][data-empty] ~ .address_nav {
    display: none;
  }
  .addresses_list[finish][data-empty] {
    margin: 0;
  }
  .empty_text {
    margin: 64px 0 32px;
  }
  .address_form_container {
    width: calc(420px + var(--page-spacing) * 2);
  }

  @media (max-width: {{settings.breakpoint}}px) {
    .address_form_container {
      width: 100%;
    }
    .addresses_list {
      grid-template-columns: 1fr !important;
    }
    .addresses_list {
      margin: 20px 0 0 0;
      gap:16px;
      grid-template-columns: 1fr;
    }
    .add_address_md {
      position: sticky;
      padding: 10px 20px;
      margin-top: 30px;
      bottom: 0;
      margin-bottom: 10px;
    }
    .address_form {
      width: 100% !important;
    }
    .address_form_header {
      margin: 24px 0 20px;
    }
    .empty_text {
      margin: 91px 0 24px;
    }
  } 
</style>
